# ProteusClone — سند تحویل پروژه برای ادامه در چت جدید

این سند رو مستقیم به Claude توی چت جدید بده (یا کپی/پیست کن اول پیام). هدف اینه که
جلسه‌ی بعدی بدون نیاز به کاوش دوباره‌ی کل کدبیس، دقیقاً بدونه چی ساخته شده، چرا
اون‌جوری ساخته شده، و چطور می‌تونه ادامه بده بدون خراب کردن چیزی که کار می‌کنه.

## ⚠️ مهم‌ترین نکته قبل از هرچیز

**هیچ‌کدوم از این کدها واقعاً کامپایل نشدن.** توی این سندباکس Qt6 نصب نبود، پس همه‌ی
کارها با مرور دستی دقیق کد (تطبیق signature، include ها، تعادل آکولاد/پرانتز،
اعتبارسنجی XML فایل‌های .ui) انجام شده، نه با gcc/qmake واقعی. اولین کاری که باید
توی چت جدید انجام بشه:

1. کاربر باید یه **Clean کامل + Rebuild** با Qt Creator یا CMake بزنه.
2. اگه خطای کامپایل داد، خروجی کامل کامپایلر رو بده تا رفع بشه.
3. تا وقتی این تایید نشده، **به فازهای جدید اضافه نکن** — چون خطاهای کوچیک syntax
   می‌تونن جمع بشن و پیدا کردنشون بعداً سخت‌تر میشه.

**به‌روزرسانی (بعد از فاز ۷):** توی جلسه‌ای که فاز ۷ (LCD+Keypad) اضافه شد، از کاربر
صریحاً پرسیده شد که آیا Build زده یا نه؛ جواب داد **هنوز نه** و خواست با احتیاط ادامه
داده بشه. یعنی الان **پنج فاز پشت‌سرهم (۶ تا ۱۰) هیچ‌کدوم واقعاً کامپایل نشدن**
(فازهای ۸، ۹، و ۱۰ توی همون جلسه، پشت‌سرهم، بدون پرسیدن دوباره اضافه شدن - چون
هنوز داخل همون جلسه‌ی «با احتیاط ادامه بده» بودیم). این سندباکس هم مثل قبل Qt6/CMake
نداشت (شبکه به مخزن‌های apt هم بسته بود، 403 گرفتم موقع تست)، پس تنها ابزار تاییدی
که این‌بار اضافه شد یه سری چک استاتیک دستی بود (تعادل آکولاد/پرانتز روی همه فایل‌ها،
تطبیق دقیق `setComponentType` با `.name` توی `librarymanager.cpp`، و برای فازهای
۸/۹/۱۰ چندتا شبیه‌ساز کامل پایتونی از منطق MCU (+Keypad یا +LCD) که نشون داد همه‌ی
فایل‌های Hex نمونه - `blink_p0_0.hex`, `keypad_scan_demo.hex`,
`keypad_scan_branching_demo.hex`, `lcd_hi_demo.hex`, `keypad_full_scan_demo.hex` -
واقعاً همون چیزی رو تولید می‌کنن که ادعا میشه، از جمله تست هر ۱۶ کلید Keypad
جداگانه برای فاز ۱۰) — نه یه کامپایلر واقعی. **این فاصله از آخرین Build واقعی
داره خیلی زیاد میشه؛ اگه جلسه بعدی هم بدون تایید Build شروع شد، به‌جد به کاربر
پیشنهاد بده که قبل از هر فاز جدید (مخصوصاً قبل از شبیه‌سازی آنالوگ که ریسک عددی
داره) یه Clean+Rebuild واقعی بزنه.**

**به‌روزرسانی مهم (قبل از فاز ۱۱):** درست قبل از شروع فاز ۱۱ (شبیه‌سازی آنالوگ)،
از کاربر دوباره صریحاً پرسیده شد - این‌بار مشخصاً به این خاطر که آنالوگ ریسک
عددی/ریاضی داره که برخلاف باگ‌های منطقی فازهای ۶ تا ۱۰، با مرور دستی سخت‌تر پیدا
میشه. کاربر این‌بار جواب داد **«Build زدم، برو سراغ آنالوگ»** - یعنی کدهای فاز ۶
تا ۱۰ واقعاً کامپایل شدن و روی سیستم خودش تست شدن. این خیلی خبر خوبیه: شمارش
«فازهای بدون Build» از اینجا دوباره از صفر شروع می‌شه.

**به‌روزرسانی (بعد از فاز ۱۱، قبل از فاز ۱۲):** برای فاز ۱۲ (Battery+ADC+DAC)
کاربر صریحاً گفت **«no need to build on qt6»** - یعنی از این به بعد تا اطلاع
بعدی، تاییدیه‌ی Build رو خودت نخواه مگر اینکه ریسک واقعاً بالا بره (مثلاً قبل از
خازن/سلف که خودشون گسسته‌سازی زمانی/ریسک عددی جدید دارن، شاید بازم ارزششو داشته
باشه یه بار دیگه بپرسی، ولی زیاد ازش استفاده نکن چون کاربر ترجیحش رو گفته).
یعنی فاز ۱۱ (تاییدنشده از قبل) + فاز ۱۲ (تازه اضافه شده) هر دو هنوز کامپایل
واقعی ندیدن، ولی این عمداً و با رضایت کاربره، نه یه ریسک ناخواسته.

## پروژه چیه

یه شبیه‌ساز مدار الکتریکی شبیه Proteus، به زبان Qt6/C++ با CMake، برای یه پروژه
درسی دانشگاهی (سبک شریف، برنامه‌نویسی شی‌گرا). کاربر یه اسپک ۲۴ صفحه‌ای کامل داره
(فایل `propozalOfphase6.txt` توی ریشه‌ی پروژه هست) که شامل ۱۱ بخشه: منوی شروع،
بوم اصلی، کتابخانه قطعات، جابجایی/ویرایش، سیم‌کشی، کتابخانه پایه (منابع/غیرفعال/
تعاملی/گیت‌ها)، کتابخانه پیشرفته (ADC/DAC/میکروکنترلر/LCD/Keypad)، کنترل شبیه‌سازی،
ابزار اندازه‌گیری، مدیریت فایل، و DRC.

کاربر صریحاً خواسته:
- تغییرات ساختاری مشکلی نداره (آزادی کامل در ری‌فکتور).
- معماری **«مثل شرکت‌ها»**: هر فایل مسئول یک بخش مشخص از برنامه.
- معمولاً فقط می‌گه «ادامه بده» بدون مشخص کردن فاز بعدی — انتخاب فاز با خودمه.

## معماری و قراردادهای پوشه‌بندی (حتماً رعایت کن)

```
/                    ← کلاس‌های هسته (Component, Pin, Node, Net, Wire, Junction,
                       CircuitScene, MainWindow) + همه‌ی قطعات مدار
/commands/           ← الگوی Command برای Undo/Redo (QUndoCommand)، هرکدوم یک فایل
/io/                 ← فقط ذخیره/بارگذاری پروژه (ProjectSerializer)
/sim/                ← فقط شبیه‌سازی (SimulationEngine, DesignRuleChecker, SimulationLogger)
```

**قانون include بین پوشه‌ها (خیلی مهم، باعث چند باگ واقعی شده بود):**
فایل‌های داخل `commands/`, `io/`, `sim/` برای include کردن هدرهای ریشه باید از
`../` استفاده کنن (مثلاً `#include "../circuitscene.h"`)، **نه** فقط `"circuitscene.h"`.
این پروژه هیچ `CMAKE_INCLUDE_CURRENT_DIR` یا `target_include_directories` سراسری
نداره، پس include نسبی-به-فایل تنها راه درستشه. برعکسش (فایل‌های ریشه که هدر زیرپوشه
رو include می‌کنن) با مسیر کامل درسته: `#include "sim/simulationlogger.h"`.

## چک‌لیست حیاتی هر بار که یه Component جدید اضافه می‌کنی

این چندتا قانونِ به‌ظاهر کوچیک، منبع چندتا باگ واقعی توی این پروژه بودن:

1. **`setComponentType(...)` باید دقیقاً همون رشته‌ای باشه که توی `librarymanager.cpp`
   به‌عنوان `.name` ثبت شده.** این رشته هم برای نمایش توی درخت کتابخانه استفاده میشه،
   هم برای `ComponentFactory::create()` (که Save/Load بهش وابسته‌ست). اگه این دو رشته
   فرق کنن، Save/Load برای اون قطعه silently خراب میشه (این دقیقاً برای
   DCVoltage/TransistorNPN/GateAND توی این پروژه اتفاق افتاده بود).
2. **`Component::setProperty(key, value, ...)` نوع رو خودکار با `value.typeName()`
   تشخیص میده.** فقط سه نوع توی `DynamicPropertiesDialog` پشتیبانی میشه:
   `"QString"`, `"double"`, `"bool"` (دقیقاً همین رشته‌ها، حروف‌بزرگ/کوچک مهمه).
   اگه یه نوع دیگه پاس بدی (مثلاً `int`)، فیلدش توی دیالوگ ویژگی‌ها اصلاً ظاهر نمیشه،
   بدون هیچ خطایی.
3. اگه قطعه‌ی دیجیتال جدیده (منطق واقعی داره، نه فقط گرافیکی)، از `DigitalComponent`
   ارث‌بری کن نه مستقیم از `Component`. اگه رفتارش ساده/ترکیبیه (خروجی = تابعی از
   ورودی‌های فعلی)، فقط `computeOutput()` رو بازنویسی کن. اگه حالت‌دار/خودمختاره
   (فلیپ‌فلاپ، کلاک، MCU)، `simulationTick()` و `resetSimulation()` رو کامل بازنویسی کن.
4. پایه‌ها رو با `setDirection(PinDirection::Input/Output/Bidirectional)` مشخص کن.
   فقط پایه‌های `Output` توی `Node::resolvedValue()` در نظر گرفته می‌شن. پیش‌فرض
   `Bidirectional` هست (یعنی نه ورودی نه خروجی محسوب میشه) — قطعات آنالوگ (R/L/C/
   دیود/ترانزیستور) عمداً این پیش‌فرض رو دارن چون هنوز موتور آنالوگ نداریم.
5. توی `librarymanager.cpp` ثبتش کن (include هدر جدید در بالای فایل + یه
   `ComponentInfo` جدید با `.name`/`.category`/`.description`/`.creator`).
6. توی `CMakeLists.txt` فایل‌های `.h`/`.cpp` جدید رو اضافه کن (بخش لیست سورس‌ها).

## نقشه کامل داده‌ای (برای فهمیدن سریع رابطه کلاس‌ها)

```
Component (پایه انتزاعی، QGraphicsObject)
  ├─ toJson()/fromJson() از قبل کامله (شامل id/name/type/category/pos/rotation/properties)
  ├─ m_properties: QMap<QString, ComponentProperty> — سیستم ویژگی پویا
  ├─ pins(): QList<Pin*>
  └─ زیرکلاس‌ها: Resistor/Capacitor/Inductor (passives.h)، Diode/TransistorNPN
     (semiconductors.h)، Ground/DCVoltage/Battery/ClockGenerator (sources.h)،
     GateAND/OR/NOT/NAND/XOR/DFlipFlop (logicgates.h)، Switch/PushButton/LED
     (interactivecomponents.h)، LogicProbe/DigitalVoltmeter/Oscilloscope/
     AnalogVoltmeter/AnalogAmmeter (measurementtools.h)، MCU (mcu.h)، LCD
     (lcd.h)، Keypad (keypad.h)، ADC (adc.h)، DAC (dac.h)

DigitalComponent : public Component  (digitalcomponent.h/cpp)
  ├─ computeOutput() — قطعات ترکیبی ساده این رو بازنویسی می‌کنن
  ├─ simulationTick() — پیش‌فرض: تاخیر انتشار + اعمال computeOutput؛ قطعات
  │  حالت‌دار (DFlipFlop, ClockGenerator, MCU) کامل بازنویسیش می‌کنن
  └─ resetSimulation()

Pin : public QGraphicsObject (pin.h/cpp)
  ├─ direction(): PinDirection {Input, Output, Bidirectional}
  ├─ drivenValue(): LogicValue {Low, High, Undefined} — فقط برای پایه‌های Output معنا داره
  ├─ node(): Node* — گره الکتریکی متصل (nullptr اگه رها/Floating باشه)
  └─ wires(): QList<Wire*>

Node (node.h/cpp) — گره هم‌پتانسیل، ساختار داده مفهومی (نه آیتم گرافیکی)
  ├─ resolvedValue(bool *hasConflict): مقدار منطقی گره را از همه‌ی پایه‌های Output
  │  متصل جمع می‌کند؛ اگه دو خروجی مقدار متفاوت بدن → Undefined + conflict=true
  ├─ pins(), wires(), net(), junction()
  └─ هر Node دقیقاً یک Junction و یک Net داره (۱:۱، حتی برای یه اتصال ساده دوتایی)

Wire : public QGraphicsPathItem (wire.h/cpp)
  ├─ startPin()/endPin() — ثابت، از سازنده تا نابودی عوض نمیشن
  ├─ rebuildGeometry() — روتینگ ۹۰ درجه با یک خم خودکار (نه چندقسمتی/قابل‌کشیدن)
  ├─ ItemIsSelectable فعاله (قابل انتخاب/حذفه)
  └─ paint(): رنگ زنده بر اساس resolvedValue() گره (قرمز=High آبی=Low بنفش=تداخل)

CircuitScene (circuitscene.h/cpp) — کنترلر مرکزی مدار
  ├─ components(), wires(), nodes() — دسترسی فقط‌خواندنی برای io/ و sim/
  ├─ connectPins(a,b) — ساخت سیم نو بین دو پایه (ابزار سیم‌کشی + بارگذاری پروژه)
  ├─ reattachWire(wire) / disconnectWire(wire) — اتصال/جداسازی یک سیم موجود با
  │  مدیریت درست Node/Net/Junction (شامل splitNodeIfNeeded برای شکستن گره وقتی
  │  یه سیم که تنها راه اتصال بود حذف میشه — الگوریتم BFS)
  └─ resetCircuit() — پاک‌سازی کامل برای پروژه جدید/بارگذاری
```

## فاز‌های تکمیل‌شده (به ترتیب زمانی)

### فاز ۰ — دیباگ اولیه (قبل از اینکه توسعه شروع بشه)
کاربر یه zip از یه کدبیس نیمه‌کاره با گزارش‌های فنی AI-generated (که غلط بودن) آورد.
پیدا و رفع شد:
- کلاس `ComponentPropertiesDialog` ناموجود که کامپایل رو می‌شکست (به‌جاش
  `CircuitScene::mouseDoubleClickEvent` با `DynamicPropertiesDialog` مسئول دیالوگ ویژگی‌هاست)
- `on_actionOpen_triggered`/`on_actionSave_triggered` تعریف‌نشده (خطای لینکر)
- عدم تطابق `componentType` با نام ثبت‌شده در LibraryManager برای ۳ قطعه
- فایل‌های یتیم duplicate (`resistor.cpp`, `capacitor.cpp`, `diode.cpp`,
  `dcsource.cpp`, `twoterminalcomponent.cpp`) که در CMakeLists کامنت بودن ولی
  کاربر فکر می‌کرد داره روشون کار می‌کنه — **حذف شدن**.
- mismatch فیلد `label` در برابر `displayName` در `ComponentProperty`

### فاز ۱ — Save/Load + DRC پایه + منوی File
- `io/projectserializer.{h,cpp}`: Save/Load با JSON، سیم‌ها بر اساس
  (شناسه‌قطعه + اندیس‌پایه) سریالایز می‌شن نه UUID پایه (چون پایه‌ها موقع بازسازی
  UUID جدید می‌گیرن)
- `sim/designrulechecker.{h,cpp}`: پایه رها + هشدار اتصال کوتاه توپولوژیک ساده
- `sim/simulationlogger.{h,cpp}`: لاگر Pub/Sub، بدون وابستگی به UI
- منوی File کامل (New/Open/Save/Save As/Export Image)، پنل Log پایین صفحه

### فاز ۲ — موتور شبیه‌سازی دیجیتال
- `pin.h`: `PinDirection`, `LogicValue` enum ها اضافه شدن
- `node.h/cpp`: `resolvedValue()` اضافه شد
- `digitallogic.h`: عملگرهای منطقی خالص (logicAnd/Or/Not/Nand/Xor) که Undefined
  رو درست منتشر می‌کنن
- `digitalcomponent.h/cpp`: کلاس پایه با تاخیر انتشار
- `logicgates.h/cpp`: AND (ارتقا)، OR، NOT، NAND، XOR، DFlipFlop
- `interactivecomponents.h/cpp`: Switch، PushButton (کنترل از دیالوگ ویژگی‌ها نه
  کلیک مستقیم روی بوم — این یه محدودیت شناخته‌شده‌ست)، LED
- `sources.h/cpp`: ClockGenerator اضافه شد؛ Ground/DCVoltage به‌عنوان منبع دیجیتال
  هم عمل می‌کنن (Ground=Low، پایه + DCVoltage=High، پایه - =Low) — تقریب عمدی
  برای اینکه بشه بدون موتور آنالوگ کامل، مدار دیجیتال تست کرد
- `sim/simulationengine.h/cpp`: Run/Pause/Stop/Step، QTimer با interval=150ms
- `wire.cpp`: paint() رنگ زنده نشون میده
- `dynamicpropertiesdialog.cpp`: پشتیبانی bool (QCheckBox) اضافه شد

### فاز ۳ — تکمیل سیم‌کشی
- `Wire::ItemIsSelectable` فعال شد + نشانگر بصری انتخاب (نقطه‌چین) در paint()
- `CircuitScene::disconnectWire`/`reattachWire`/`splitNodeIfNeeded` (الگوریتم BFS
  برای شکستن/ادغام درست گره‌ها — جزئیات کامل توی خود کد کامنت‌گذاری شده)
- `commands/deletewirecommand.{h,cpp}`: حذف/بازگردانی سیم با Undo کامل
- `commands/deletecomponentcommand.cpp`: ارتقا یافت تا سیم‌های متصل به قطعه‌ی
  حذف‌شده رو هم درست جدا/بازوصل کنه (قبلاً orphan می‌موندن)

### فاز ۴ — رابط کاربری
- `circuitgraphicsview.h/cpp`: کلاس جدید (promoted widget در .ui) — زوم
  (Ctrl+چرخ‌ماوس)، Pan (نگه‌داشتن Space)، سیگنال مختصات زنده
- جستجوی کتابخانه واقعاً کار می‌کنه (`on_lineEditSearch_textChanged`)
- `commands/mirrorcommand.{h,cpp}`: Mirror افقی با کلید M (self-inverse، مثل Rotate
  فقط کیبوردیه، **QAction با شورتکات M عمداً اضافه نشد** چون با keyPressEvent دستی
  تداخل می‌کنه و شورتکات ناخواسته می‌قاپه)
- `startupdialog.h/cpp` + تغییر `main.cpp`: دیالوگ New Project (اندازه بوم)/
  Open Existing قبل از باز شدن MainWindow

### فاز ۵ — ابزار اندازه‌گیری
- `measurementtools.h/cpp`: LogicProbe، DigitalVoltmeter (نگاشت منطقی→ولتاژ بر
  اساس یه property قابل‌تنظیم، نه اندازه‌گیری آنالوگ واقعی)، Oscilloscope دو کاناله
  (بافر ۶۴ نمونه‌ای، رسم موج پله‌ای) — **آمپرمتر عمداً ساخته نشد** چون بدون موتور
  آنالوگ جریان واقعی وجود نداره که نشون بده
- `SimulationEngine::tick()` هر Oscilloscope رو sample می‌کنه

### فاز ۶ — میکروکنترلر (آخرین فاز قبل از این handoff)
- `component.h/cpp`: قلاب `populateContextMenu(QMenu*)` + `contextMenuEvent()`
  اضافه شد (برای اینکه MCU بتونه "Load HEX File..." رو به راست‌کلیک اضافه کنه،
  بدون تاثیر روی بقیه قطعات چون پیش‌فرضش خالیه)
- `mcu.h/cpp`: PC، ۴ رجیستر، ۲۵۶ بایت RAM، دو پورت I/O هشت‌بیتی (P0=پایه۰-۷،
  P1=پایه۸-۱۵، **فقط خروجی، نه دوطرفه**)، دیکودر ۵ دستور مستند‌شده (NOP/SETB/CLR/
  MOV/ADD/JMP/HALT — فرمت دقیق بایت‌ها بالای `mcu.h` کامنت شده)، `loadHexFile()`
  با پارسر Intel HEX واقعی (record type 00/01، **checksum اعتبارسنجی نمی‌شه** —
  ساده‌سازی عمدی)
- یه فایل نمونه `blink_p0_0.hex` تولید و با پایتون verify شد (چشمک‌زن P0.0)

### فاز ۷ — LCD ۱۶×۲ و Keypad ۴×۴ (بدون تایید Build فاز ۶، به درخواست صریح کاربر)
- `lcd.h/cpp` (کلاس `LCD`): ۱۰ پایه Input (D0-D7 + RS + EN). لبه بالارونده EN
  لچ می‌شه (دقیقاً الگوی `DFlipFlop::simulationTick` - مقایسه `m_lastEnable` قبلی
  با فعلی). فرمت دستور (وقتی RS=Low): `0x01`=Clear، `0x02`=Return Home،
  `0x80|pos`=Set Cursor (ساده‌شده نسبت به Set DDRAM Address واقعی HD44780،
  مستند شده بالای `lcd.h`). وقتی RS=High: بایت به‌عنوان ASCII نوشته میشه و
  مکان‌نما با گردش خودکار جلو میره. دستور ناشناخته → هشدار توی SimulationLogger.
  فقط ASCII پایه (۰-۱۲۷) قابل‌اعتماده (مثل خودِ HD44780 واقعی).
- `keypad.h/cpp` (کلاس `Keypad`): ۴ پایه سطر Output (اندیس ۰-۳) + ۴ پایه ستون
  Input (اندیس ۴-۷). کلید «فشرده‌شده» با تایپ نامش (`"1".."9"`,`"A".."D"`,`"*"`,
  `"#"`, یا `"None"`) توی ویژگی `pressedKey` (دیالوگ ویژگی‌ها) انتخاب میشه —
  عمداً همون الگوی Switch/PushButton (نه کلیک مستقیم روی بوم، محدودیت شناخته‌شده
  #۷ پایین همین لیست). هر تیک: اگه ستونِ کلیدِ انتخاب‌شده هم‌اکنون High اسکن
  بشه، سطرش High میشه؛ وگرنه Low (یا Undefined اگه هیچ ستونی درایو نشده).
- هر دو توی `librarymanager.cpp` (category="Advanced", کنار MCU) و `CMakeLists.txt`
  ثبت شدن. **هیچ فایل دیگه‌ای لازم نبود دست بخوره** — چون `ComponentFactory` از
  همون لیست `LibraryManager` پر میشه (نه رشته‌ی دستی جدا)، Save/Load و DRC
  (پایه رها شده) به‌صورت خودکار این دو قطعه رو هم پوشش میدن؛ تایید شد با خوندن
  `io/projectserializer.cpp` و `sim/designrulechecker.cpp`.
- **محدودیت مهم و هنوز حل‌نشده:** چون پورت‌های MCU فقط خروجی‌ان (نکته #۳ همین
  لیست پایین)، MCU نمی‌تونه سطرهای Keypad رو واقعاً بخونه — یعنی چرخه کامل
  اسکن رفت‌وبرگشتی از طریق MCU هنوز قابل تست نیست. خودِ منطق اسکن Keypad
  (با وصل چندتا Switch به پایه‌های ستون + Logic Probe روی سطرها) مستقل و
  کامله. همچنین برای LCD یه فایل hex نمونه ساخته نشد — نوشتن حتی یه کاراکتر
  از طریق MCU نیاز به ۸ تا `SETB`/`CLR` پشت‌سرهم (برای ساخت بایت روی P0) +
  چندتا برای RS/EN داره؛ با ISA فعلی MCU ممکنه ولی خسته‌کننده‌ست و اینجا
  ساخته نشد.

### فاز ۸ — ورودی دوطرفه واقعی پورت‌های MCU (رفع مهم‌ترین محدودیت فاز ۷)
- `mcu.h/cpp`: دو فیلد جدید `m_portP0Dir`/`m_portP1Dir` (پیش‌فرض `0xFF` یعنی
  همه Output — کاملاً سازگار با فازهای قبلی/`blink_p0_0.hex`). دو دستور جدید:
  - `0x03 port bit dir` → **SETDIR**: جهت یک بیت مشخص از یک پورت رو Input/Output
    می‌کنه (۴ بایت).
  - `0x04 reg port bit` → **IN**: مقدار لحظه‌ای گره‌ی وصل به اون پایه رو می‌خونه
    (High→۱, Low→۰, Undefined→۰ + هشدار در SimulationLogger) و توی یه رجیستر
    می‌ریزه (۴ بایت).
  فرمت کامل بالای `mcu.h` مستند شده (دنبال همون الگوی قبلی: تعداد بایت هر
  دستور خودش تصمیم می‌گیره چقدر `m_pc` جلو بره، پس اضافه‌کردن دستور ۴بایتی
  کنار دستورهای ۳بایتی قبلی مشکلی نداشت).
- `updatePortPins()` بازنویسی شد: برای هر بیت، اول جهتش رو از `m_portP0Dir`/
  `m_portP1Dir` می‌خونه و `Pin::setDirection()` رو صداش می‌زنه؛ فقط اگه
  Output باشه `setDrivenValue()` صدا زده میشه (پایه‌ی Input دیگه گره‌ی خودش
  رو نمی‌رونه - دقیقاً طبق قانون موجود `Node::resolvedValue()` که فقط پایه‌های
  Output رو می‌بینه، **هیچ تغییری توی `pin.h`/`node.h`/`node.cpp` لازم نبود**).
- **تایید شد، نه فقط ادعا:** یه برنامه‌ی Hex نمونه (`keypad_scan_demo.hex`،
  جدا از zip، مثل خودِ `blink_p0_0.hex`) نوشتم که P0.0-P0.3 رو Input می‌کنه،
  ستون ۰ (P1.0) رو HIGH و بقیه ستون‌ها رو LOW می‌کنه، بعد هر ۴ سطر رو با IN
  توی R0-R3 می‌خونه. کل منطق (MCU + Keypad) رو با یه شبیه‌ساز پایتون مو‌به‌مو
  از روی همین کد C++ تکرار کردم: با فرض `pressedKey="1"` (که طبق چیدمان
  Keypad یعنی row0/col0) نتیجه دقیقاً `R0=1, R1=R2=R3=0` شد - یعنی زنجیره‌ی
  MCU→Keypad→MCU (اسکن ستون → روشن‌شدن سطر → خواندن سطر) اولین‌بار توی این
  پروژه سرتاسر کار می‌کنه، نه فقط تک‌تک قطعات.
- **هنوز باقی‌مونده:** این فقط یک اسکن تک‌ستونه برای اثبات مفهوم؛ یه حلقه‌ی
  اسکن کامل ۴ ستونه (که نیاز به شرط/پرش شرطی داره و ISA فعلی MCU پرش شرطی
  ندارد، فقط `JMP` بی‌قید) فاز جداگانه‌ایه — به لیست پایین اضافه شد.

### فاز ۹ — پرش شرطی (JNZ/JZ) + دمو واقعیِ شاخه‌گیری Keypad + دمو LCD (ادامه همون جلسه)
- `mcu.h/cpp`: دو دستور جدید `0x31 reg addrHi addrLo` = **JNZ** (پرش اگه رجیستر
  ≠۰) و `0x32 reg addrHi addrLo` = **JZ** (پرش اگه رجیستر =۰)، هر دو ۴ بایتی،
  دقیقاً کنار `JMP` (۰x30) توی همون گروه پرش‌ها. این اولین ابزار تصمیم‌گیری
  شرطیِ این ISA هست - قبلش برنامه فقط می‌تونست خطی یا حلقه‌ی بی‌قید (JMP به
  عقب) اجرا کنه.
- **`keypad_scan_branching_demo.hex`** (جدا از zip): برنامه‌ای که ستون ۰ رو
  HIGH می‌کنه، ردیف۰ رو با IN می‌خونه، اگه ۱ بود با JNZ می‌پره به «پیدا شد
  توی ستون ۰» (`R1=0`)؛ وگرنه ستون ۱ رو امتحان می‌کنه و اگه اونجا پیدا شد
  `R1=1`؛ وگرنه `R1=0xEE` (یعنی پیدا نشد). **تایید شد با شبیه‌ساز پایتونِ
  کامل mcu+keypad، روی ۴ سناریو (کلید در ستون۰، کلید در ستون۱، هیچ کلیدی،
  کلید روی ردیفی که این دمو چکش نمی‌کنه) - هر ۴ تا دقیقاً همون نتیجه‌ای
  رو دادن که انتظار می‌رفت.**
  - محدودیت صادقانه: چون `SETB`/`CLR`/`IN` فقط با بیت *ثابت* کار می‌کنن (نه
    بیتِ اندیس‌دار از روی رجیستر)، این دمو فقط ۲ ستون و ۱ ردیف رو چک می‌کنه
    (نه هر ۱۶ کلید رو) - برای یه اسکنر کامل ۴×۴ باید یا کد رو کامل Unroll
    کرد (که حجمش از ۲۵۶ بایت ROM رد میشه - امتحان کردم، حدود ۳۴۶ بایت شد)
    یا آدرس‌دهی غیرمستقیم پورت (بیت از روی مقدار رجیستر) به ISA اضافه کرد؛
    این دومی به لیست پایین اضافه شد.
- **`lcd_hi_demo.hex`** (جدا از zip): با استفاده از دستورات از قبل تایید‌شده
  (`SETB`/`CLR`/`NOP`، بدون نیاز به هیچ دستور جدید)، D0-D7 رو به P0 و RS/EN
  رو به P1.0/P1.1 وصل می‌کنه، دستور Clear Display (`0x01`) می‌فرسته، بعد
  کاراکترهای 'H' و 'I' رو می‌نویسه. نکته مهم پیاده‌سازی: بین `SETB` کردن EN
  و `CLR` کردنش یه `NOP` اضافه گذاشتم چون ترتیب تیک‌خوردن قطعات مختلف توی
  `SimulationEngine::tick()` (از `m_scene->items()`) تضمین‌شده نیست - بدون
  این NOP اضافه، اگه LCD قبل از MCU تیک بخوره ممکنه لبه EN رو یه تیک دیر
  ببینه. **تایید شد با هم‌شبیه‌سازی پایتونیِ MCU+LCD در هر دو ترتیب ممکن
  (اول MCU یا اول LCD) - هر دو دقیقاً `"HI"` روی سطر اول تولید کردن.**

### فاز ۱۰ — OUTPORT/INPORT + اسکنر واقعی و کامل هر ۱۶ کلید Keypad (ادامه همون جلسه)
- `mcu.h/cpp`: دو دستور جدید `0x05 port reg` = **OUTPORT** (کل بایت پورت رو از
  رجیستر می‌نویسه) و `0x06 reg port` = **INPORT** (کل بایت پورت رو از پایه‌ها
  می‌خونه، معادل ۸ بار `IN` پشت‌سرهم). این دقیقه‌ای/بیتی نیست (هنوز نمیشه گفت
  «بیت شماره‌ی توی این رجیستر») ولی برای «فقط یکی از ۴ ستون رو در یک لحظه HIGH
  کن» کاملاً کافیه: یه الگوی one-hot با `MOV` می‌سازی و با یه `OUTPORT` کل پورت
  رو یک‌جا می‌نویسی، به‌جای ۴ تا `SETB`/`CLR` جدا.
- **`keypad_full_scan_demo.hex`** (جدا از zip): این‌بار یه اسکنر واقعی و کامل -
  هر ۴ ستون رو یکی‌یکی HIGH می‌کنه (با `OUTPORT`)، زیر هرکدوم هر ۴ ردیف رو با
  `IN`+`JNZ` چک می‌کنه، و اگه کلیدی پیدا شد اندیسش (۰ تا ۱۵، `row*4+col`) رو
  توی `R1` می‌ذاره و `HALT` می‌کنه؛ اگه هیچ‌کدوم نبود `R1` روی سنتینل `0xFF`
  می‌مونه. کل برنامه ۲۳۶ بایته (زیر سقف ۲۵۶ بایت ROM - چیزی که نسخه‌ی قبلی‌اش
  بدون `OUTPORT` بهش نمی‌رسید، فاز ۹ رو ببین).
  **تایید شد، نه فقط ادعا:** با یه هم‌شبیه‌سازی پایتونیِ کامل MCU+Keypad، هر
  ۱۶ کلید (۱ تا ۹، A تا D، `*`، `0`، `#`) رو یکی‌یکی «فشار داده» تست کردم - هر
  ۱۶ تا دقیقاً اندیس درست خودشون رو توی R1 تولید کردن، به‌علاوه‌ی حالت «هیچ
  کلیدی فشرده نشده» که درست `0xFF` داد. یعنی اولین‌بار توی این پروژه، MCU واقعاً
  می‌تونه بفهمه کدوم یکی از ۱۶ کلید Keypad فشرده شده - نه فقط یک یا دو تا.
- توی `keypad_scan_demo.hex` (فاز ۸) و `keypad_scan_branching_demo.hex` (فاز ۹)
  دست نزدم؛ هنوز به‌عنوان نمونه‌های ساده‌تر و آموزشی از مراحل قبلی نگه داشته
  شدن (اولی فقط بیدایرکشنال بودن رو نشون میده، دومی فقط JNZ رو، بدون OUTPORT).

### فاز ۱۱ — شبیه‌سازی آنالوگ DC واقعی (Resistor+DC Source+Ground) + Voltmeter/Ammeter واقعی
این فاز عمداً محدود نگه داشته شد (طبق پیشنهاد خودِ همین سند قبل از این فاز):
فقط مدارهای **خطی و DC** با مقاومت + منبع ولتاژ ایده‌آل + زمین. خازن/سلف/دیود/
ترانزیستور صراحتاً خارج از این فاز موندن (به لیست بالا نگاه کن).

- **`node.h`**: یک فیلد ولتاژ مستقل به `Node` اضافه شد (`hasVoltage()`/`voltage()`/
  `setVoltage()`/`clearVoltage()`) - کاملاً جدا از `resolvedValue()` دیجیتالی
  موجود و بدون هیچ تداخلی باهاش (قطعات آنالوگ پایه‌هایشان پیش‌فرض Bidirectional
  هستند، پس اصلاً در `resolvedValue()` شرکت نمی‌کنند - این طراحی از قبل، حتی
  قبل از فاز ۱۱، توی کامنت‌های `pin.h` مستند شده بود، انگار خودِ کدبیس منتظر
  همین فاز بوده).
- **`sim/analogsolver.h/cpp`** (فایل جدید، کلاس `AnalogSolver`): مدار را با
  Union-Find به زیرمدارهای جدا از هم تقسیم می‌کند (چند شبکه غیرمتصل روی یک بوم
  ممکنه باشه)، هرکدوم رو جدا با روش Modified Nodal Analysis (مقاومت‌ها به‌صورت
  رسانایی، منابع ولتاژ با متغیر جریان کمکی طبق روش استاندارد MNA) به یک دستگاه
  معادلات خطی تبدیل می‌کنه، و با حذف گاؤسی + Partial Pivoting حلش می‌کنه.
  زیرمداری که هیچ Ground‌ای نداشته باشه، دست‌نخورده (`hasVoltage()==false`)
  می‌مونه - نه صفر، نه مقدار کهنه‌ی تیک قبلی (هر تیک همه‌چیز اول پاک میشه).
- **`AnalogVoltmeter`/`AnalogAmmeter`** (اضافه شد به `measurementtools.h/cpp`،
  کنار `DigitalVoltmeter` قدیمی که دست‌نخورده موند برای سازگاری با پروژه‌های
  ذخیره‌شده‌ی قبلی): ولت‌متر فقط بعد از حل شدن مدار `Node::voltage()` دو پایه‌ش
  رو می‌خونه (خودش هیچ اثری روی مدار نداره - ولت‌متر ایده‌آل). آمپرمتر جالب‌تره:
  توی خودِ `AnalogSolver` **دقیقاً مثل یک منبع ولتاژ ۰ ولت** مدل میشه (افت ولتاژ
  صفر روی آمپرمتر ایده‌آل)، و جریان مجهولِ کمکیِ همون منبع در MNA، همون جریانیه
  که از آمپرمتر عبور کرده - ترفند استاندارد شبیه‌سازهای اسپایس، نه یه فرمول
  دستی جدا.
- **`sim/simulationengine.cpp`**: `tick()` حالا اول `AnalogSolver::solve()` رو
  صدا می‌زنه (قبل از چرخه دیجیتال)، بعد بقیه‌ی چرخه‌ی قبلی رو دقیقاً مثل قبل
  ادامه میده. برای مدار کاملاً دیجیتالی این تقریباً رایگانه (فوراً true برمی‌گرده
  چون هیچ Resistor/DC Sourceای پیدا نمی‌کنه). `resetAllComponents()` هم آمپرمترها
  و ولتاژ گره‌ها رو پاک می‌کنه تا بعد از Stop بلافاصله ERR/-- نشون بده، نه مقدار کهنه.
- **تایید شد، نه فقط ادعا (این‌بار عددی/ریاضی، نه فقط منطقی):** الگوریتم رو
  کلمه‌به‌کلمه (همون Union-Find، همون ترتیب stamp کردن ماتریس، همون حذف گاؤسی)
  با پایتون بازنویسی کردم و ۹ سناریو تست کردم: تقسیم‌کننده ولتاژ ساده (جواب
  دستی=۵ولت، برنامه=۵ولت)، مقاومت سری+موازی، مدار شناور بدون Ground (درست fail
  میشه)، دو مدار مستقل روی یک بوم که یکی درست و یکی شناوره (هرکدوم جدا و درست
  رفتار کرد)، آمپرمتر به‌عنوان منبع ۰ولت (۱۰ولت روی ۱کیلواهم = دقیقاً ۱۰ میلی‌آمپر)،
  پل وی‌تستون متوازن (V(چپ)=V(راست) دقیقاً)، دو منبع ولتاژ مستقل روی یک شبکه
  (جواب معادله نودال دستی رو دقیقاً تکرار کرد)، و یک شبکه پلکانی ۵ مرحله‌ای
  برای پایداری عددی. **هر ۹ تا دقیقاً با جواب تحلیلی/دستی یکی شدن.**
- **محدودیت‌های صادقانه (عمداً خارج از این فاز، لیست بالا رو ببین):** خازن/سلف
  (نیاز به گسسته‌سازی زمانی)، دیود/ترانزیستور (غیرخطی)، Battery (اصلاً پیاده
  نشده)، رفتار آنالوگ Switch/PushButton/Potentiometer (فعلاً فقط دیجیتالی‌ان،
  Potentiometer اصلاً کلاسش نیست). مقاومت نامعتبر (≤۰) بی‌سروصدا در AnalogSolver
  نادیده گرفته میشه - این باید جلوتر (موقع ویرایش ویژگی) جلوگیری بشه، فاز جدایی نیست.

### فاز ۱۲ — Battery + ADC + DAC (کاربر گفت «بدون نیاز به Build روی Qt6 ادامه بده»)
- **`Battery`** (`sources.h/cpp`): دقیقاً مثل `DCVoltage` عمل می‌کنه (هم دیجیتالی
  هم در `AnalogSolver` - یک شرط `type=="DC Source" || type=="Battery"` مشترک).
  ویژگی "internalResistance" ذخیره میشه ولی هنوز در معادلات اثر نداره (به لیست
  بالا بند ۳ نگاه کن) - ساده‌سازی صادقانه، نه فراموشی.
- **`ADC`** (`adc.h/cpp`، ۱۱ پایه: Vin/Vref+/Vref- آنالوگ + D0-D7 دیجیتال، ۸بیتی
  ثابت): نگاشت خطی با اشباع دو سر بازه، `conversionDelayTicks` (بعد از تغییر
  Vin، خروجی دیجیتال با تاخیر اعمال میشه، در طول تاخیر مقدار قبلی حفظ میشه -
  دقیقاً طبق بند ۷.۱ مستند)، و اگه Vin/Vref به هیچ زیرمدار حل‌شده‌ای وصل نباشن
  (`hasVoltage()==false`)، خروجی قبلی حفظ میشه + هشدار در پنجره گزارشات (نه
  خطا، نه توقف).
- **`DAC`** (`dac.h/cpp`، ۱۲ پایه: D0-D7 دیجیتال + Vref+/Vref-/Vout+/Vout- آنالوگ):
  آینه‌ی ADC. نکته معماری: چون ولتاژ خروجی DAC هر تیک از دیجیتال محاسبه میشه
  (نه یه ویژگی ثابت مثل DC Source)، `AnalogSolver` این‌بار به‌جای خوندن
  `property("voltage")`، مستقیم `dynamic_cast<DAC*>` می‌کنه و `outputVoltage()`
  رو صدا می‌زنه - دقیقاً همون الگوی `AnalogAmmeter` ولی برعکس (این یکی می‌رونه،
  اون یکی می‌خونه).
- **محدودیت زمان‌بندی صادقانه:** چون `SimulationEngine::tick()` اول `AnalogSolver`
  رو حل می‌کنه و *بعد* `DigitalComponent`ها (از جمله DAC) تیک می‌خورن، مقداری
  که Solver از DAC می‌بینه مال تیک قبلیه - یعنی همیشه حداقل یک تیک تاخیر
  ساختاری بین تغییر دیجیتال DAC و اثرش رو شبکه آنالوگ هست، جدا از
  `conversionDelayTicks` (که رویش اضافه میشه، نه جایگزینش). عمداً دست‌نخورده
  گذاشته شد چون تغییر ترتیب Tick می‌تونست روی چیزهای دیگه‌ای که الان درستن اثر بذاره.
- **تایید شد:** نگاشت خطی ADC/DAC رو با پایتون روی چند نقطه (کمینه/بیشینه/میانی/
  خارج‌از‌بازه/Vref غیرصفر/Vref نامعتبر) و یک تست Round-trip (ADC→کد→DAC، خطا
  زیر یک پله‌ی کوانتیزاسیون) چک کردم. **یک نکته ظریف موقع تست پیدا شد و مستند
  شد، نه پنهان:** `std::lround` توی C++ نیم‌به‌بالا گرد می‌کنه (round-half-away-
  from-zero) ولی `round()` پایتون round-half-to-even هست؛ اول تستم از این
  فرق بی‌خبر بود، بعد با یه نسخه‌ی معادل `lround` توی پایتون دوباره چک کردم
  و نتیجه‌ی C++ (که خودِ کد واقعاً همینه) درست تایید شد.

### فاز ۱۳ — خازن/سلف واقعی در AnalogSolver (روش Backward Euler)
کاربر گفته بود «no need to build on qt6»، پس بدون پرسیدن دوباره جلو رفتم - ولی
دقیقاً با همون سطح احتیاط (تایید عددی سنگین) که خودِ فاز ۱۱ داشت، چون این هم
محاسبات ممیز شناوره.
- **`passives.h`**: `Capacitor`/`Inductor` هرکدوم یک فیلد حالت گذرا گرفتن -
  `Capacitor::previousVoltage()`، `Inductor::previousCurrent()` - که فقط
  توسط `AnalogSolver` خونده/نوشته میشه و با `resetTransientState()` (صدا زده
  شده از `resetAllComponents()`) روی صفر (خازن دشارژ/سلف بدون جریان) در ابتدای
  هر Run برمی‌گرده.
- **`sim/analogsolver.cpp`**: خازن به‌صورت مدل معادل نورتن Backward Euler حل
  میشه - دقیقاً مثل یک مقاومت با رسانایی Gc=C/Δt، به‌علاوه یک منبع جریان
  تاریخچه (بر پایه ولتاژ تیک قبلی) که به گره‌ها تزریق/برداشت میشه. سلف هم مثل
  یک منبع ولتاژ (مجهول جریان کمکی، دقیقاً همون تکنیک DC Source/Ammeter) ولی
  با یک جمله خودی اضافه (-L/Δt) روی قطر ماتریس و یک جمله تاریخچه (بر پایه
  جریان تیک قبلی) در سمت راست معادله - این دو جمله‌ی اضافه دقیقاً چیزیه که
  سلف رو از یک منبع ولتاژ ساده متمایز می‌کنه.
- گام زمانی: `kAnalogTimeStepSeconds=0.001` (۱ میلی‌ثانیه شبیه‌سازی‌شده به ازای
  هر تیک) - کاملاً مستقل از فاصله واقعی تایمر UI (۱۵۰ میلی‌ثانیه)؛ به لیست
  بالا (بند ۲) برای محدودیتش مراجعه کن.
- **تایید شد (عددی، دقیق‌تر از فاز ۱۱ حتی):** الگوریتم رو کلمه‌به‌کلمه با
  پایتون بازنویسی کردم و مدار RC و RL رو صدها تیک شبیه‌سازی کردم، بعد نتیجه
  رو با فرمول‌های تحلیلی شناخته‌شده مقایسه کردم:
  - شارژ خازن: `Vc(t)=V0*(1-e^(-t/RC))` - خطای نسبی زیر ۰٫۰۴٪ در همه نقاط چک‌شده.
  - تخلیه خازن (پیش‌شارژ به ۱۰ولت، بعد از طریق مقاومت تخلیه): `Vc(t)=V0*e^(-t/RC)` -
    همون سطح خطا.
  - حالت ماندگار خازن: بعد از ۲۰ ثابت زمانی، `Vc` دقیقاً به ولتاژ منبع رسید و
    جریان عبوری از مقاومت سری دقیقاً صفر شد (رفتار درست «خازن در DC مسدودکننده‌ست»).
  - افزایش جریان سلف: `I(t)=(V0/R)*(1-e^(-tR/L))` - خطای نسبی زیر ۰٫۴٪.
  خطاهای باقی‌مانده (زیر ۱٪) دقیقاً همون خطای گسسته‌سازی مرتبه‌اول مورد انتظار
  روش Backward Euler‌ان، نه نشونه‌ی باگ - با کوچیک‌تر شدن Δt نسبت به ثابت
  زمانی مدار، این خطا هم کوچیک‌تر میشه (رفتار درست یک روش عددی همگرا).
- **محدودیت‌های صادقانه:** دیود/ترانزیستور هنوز پیاده نشدن (غیرخطی، نیاز به
  نیوتن-رافسون - بند ۱ بالا). گام زمانی ثابته، نه تطبیقی (بند ۲ بالا). مقاومت
  داخلی Battery هنوز اعمال نمیشه (بند ۳ بالا، بدون تغییر نسبت به فاز ۱۲).

### فاز ۱۴ — دیود غیرخطی واقعی در AnalogSolver (معادله شاکلی + نیوتن-رافسون)
- **`semiconductors.h/cpp`**: `Diode` دو ویژگی عددی جدید گرفت -
  `saturationCurrent` (Is، پیش‌فرض ۴٫۳۵۲nA) و `idealityFactor` (n، پیش‌فرض
  ۱٫۹۰۶ - این دو تقریباً همون پارامترهای رایج SPICE برای 1N4148 هستن) - به‌علاوه
  یک فیلد حالت گذرا `previousVoltageGuess` (نقطه شروع حدس نیوتن-رافسون هر
  تیک، از آخرین مقدار همگراشده تیک قبل، نه از صفر).
- **`sim/analogsolver.cpp`**: دیود مثل خازن (فاز ۱۳) مجهول جدیدی به دستگاه
  معادلات اضافه نمی‌کنه - فقط یک رسانایی دیفرانسیلی (مشتق معادله شاکلی در
  نقطه حدس فعلی) + یک منبع جریان معادل نورتن، که دقیقاً با همون الگوی خازن
  (`b[anode]+=iEq`، `b[cathode]-=iEq`) استمپ میشه. تفاوت اصلی: چون دیود
  غیرخطیه، اگه توی زیرمدار دیود وجود داشته باشه، به‌جای یک‌بار حل کردن،
  یک حلقه نیوتن-رافسون (حداکثر ۱۰۰ تکرار، آستانه همگرایی ۱ نانوولت) اجرا
  میشه: هر تکرار، ماتریس با حدس فعلی دوباره ساخته و حل میشه، ولتاژ جدید
  دیود از جواب استخراج میشه، و اگه تغییرش از آستانه کمتر بود همگرا شده.
- **محافظ عددی مهم:** قبل از ارزیابی `exp()`، ولتاژ حدس به سقف ۱٫۵ ولت کلمپ
  میشه (دیودهای واقعی عملاً هیچ‌وقت خیلی بیشتر از این بایاس مستقیم نمی‌بینن)
  تا از سرریز نمایی (که می‌تونست NaN/Infinity تولید کنه و کل حل رو خراب کنه)
  جلوگیری بشه - این دقیقاً همون تکنیکی‌ه که شبیه‌سازهای اسپایس واقعی استفاده
  می‌کنن (هرچند اونا از یک فرمول لگاریتمی نرم‌تر استفاده می‌کنن؛ این نسخه
  ساده‌تره ولی امنیت عددی مشابهی داره).
- زیرمداری که همگرا نشه (یا سیستم خطی داخلش تکین بشه)، مثل بقیه موارد شکست،
  `hasVoltage()==false` می‌مونه + پیام خطا در پنجره گزارشات - نه کرش، نه مقدار غلط.
- **تایید شد (سخت‌گیرانه‌تر از همه فازهای قبلی):** برخلاف فازهای قبلی که فقط
  الگوریتم رو با پایتون تکرار کردم، این‌بار جواب رو با یک روش **کاملاً مستقل**
  (Bisection روی همون معادله فیزیکی، نه هیچ کدی مشترک با Newton-Raphson) مقایسه
  کردم؛ روی ۵ ترکیب مختلف ولتاژ/مقاومت، اختلاف در حد خطای ممیز شناور دستگاه
  (~1e-16) بود - یعنی عملاً یکی. علاوه بر این:
  - دیود معکوس‌بایاس: درست خاموش موند (بدون جریان محسوس).
  - همگرایی از یک حدس اولیه خیلی بد (۵ولت، دور از جواب واقعی ~۰٫۶۸ولت): بازم
    به همون جواب درست رسید - یعنی محافظ کلمپ و الگوریتم واقعاً robust‌ان.
  - دو دیود سری: ولتاژ به‌طور مساوی و منطقی بین‌شون تقسیم شد.
- **محدودیت صادقانه:** TransistorNPN هنوز کاملاً گرافیکیه، در این فاز شرکت
  نمی‌کنه (بند ۱ لیست بالا).

### فاز ۱۵ — حافظه خارجی EEPROM/RAM
- **`externalmemory.h/cpp`** (کلاس `ExternalMemory`، ۱۷ پایه: A0-A7 آدرس +
  D0-D7 داده دوطرفه + WE): دقیقاً از دو الگوی از‌قبل‌تایید‌شده استفاده می‌کنه -
  تشخیص لبه بالارونده WE برای نوشتن (عین `LCD::simulationTick`) و جهت‌دهی پویای
  پایه‌های داده بین Input/Output (عین پورت‌های دوطرفه MCU، فاز ۸). حین WE=High
  پایه‌های داده Input می‌شن و فقط روی لبه بالارونده بایت لچ میشه؛ حین WE=Low
  پایه‌های داده Output می‌شن و پیوسته بایت آدرس فعلی رو می‌رونن.
- **تصمیم صادقانه:** چون هم EEPROM (غیرفرار) و هم RAM (فرار) رو نمایندگی
  می‌کنه، طرف EEPROM رو گرفتم - `resetSimulation()` محتوای حافظه رو پاک نمی‌کنه
  (فقط وضعیت شبیه‌سازی/جهت پایه‌ها بازنشانی میشه)، دقیقاً مثل یک EEPROM واقعی.
- **تایید شد:** نوشتن+خواندن چند آدرس مستقل، عدم لچ مجدد وقتی WE بدون لبه
  جدید High می‌مونه (یعنی تغییر دیتا روی باس بدون لبه جدید اثر نمی‌ذاره)، و
  خواندن آدرس دست‌نخورده = صفر - همه با شبیه‌ساز پایتون تک‌تک تایید شدن.

### فاز ۱۶ — پیام دقیق «Floating input detected.» + رفتار آنالوگ واقعی Switch/PushButton
- **`digitalcomponent.h/cpp`**: تابع کمکی جدید `hasFloatingInput()` - همه پایه‌های
  غیر از خروجی را چک می‌کند؛ اگه هرکدوم Undefined بود، `true`. توی
  `simulationTick()` پیش‌فرض (که GateAND/OR/NOT/NAND/XOR ازش استفاده می‌کنن)،
  درست قبل از صدا زدن `computeOutput()`، اگه ورودی شناور بود پیام **دقیقاً**
  `"Floating input detected."` (به انگلیسی، عیناً طبق بند ۶.۴ مستند - نه ترجمه
  شده) توی SimulationLogger ثبت میشه. همین چک به `DFlipFlop::simulationTick()`
  هم اضافه شد (که خودش را کامل بازنویسی می‌کند، پس از منطق پیش‌فرض استفاده نمی‌کرد).
- **`interactivecomponents.h/cpp`**: کشف فاز ۱۵ («Switch/PushButton فقط یک پایه
  دارن») این‌بار با احتیاط برطرف شد - نه با تغییر پایه ۰ (که می‌تونست چیزی رو
  بشکنه)، بلکه با **اضافه کردن** دو پایه جدید (۱ و ۲) به‌عنوان ترمینال‌های
  فیزیکی کلید، در حالی که پایه ۰ و کل رفتار دیجیتالی‌اش کاملاً دست‌نخورده موند
  (`outputPinIndex()` صراحتاً ۰ برگردانده می‌شود، نه دیگر پیش‌فرض `pinCount()-1`
  که با اضافه شدن پایه‌ها عوض می‌شد). پروژه‌های قدیمی که فقط پایه ۰ رو
  استفاده می‌کردن، بدون تغییر کار می‌کنن.
- **`sim/analogsolver.cpp`**: پایه‌های ۱/۲ کلید/دکمه *بسته* دقیقاً مثل یک
  `Resistor` با مقاومت خیلی کوچک (`1e-6` اهم) استمپ می‌شوند - نه یک نوع عنصر
  جدید، فقط یک ورودی دیگر به همان `resistors` که از قبل کامل تست شده بود.
  *باز* اصلاً استمپ نمی‌شود (نه مقاومت بزرگ، واقعاً غایب - مثل بند ۶.۳ مستند
  که «مقاومت بی‌نهایت» می‌خواهد).
- **تایید شد:** با پایتون یک مدار منبع+مقاومت+کلید ساختم؛ کلید باز → دو طرفش
  دو گره کاملاً مجزا و مستقل (نه فقط مقادیر نزدیک، واقعاً دو معادله جدا)؛
  کلید بسته → عملاً هم‌ولتاژ (اختلاف در حد `1e-9` ولت) و جریان از مقاومت
  بسته دقیقاً برابر جریان بار (V/R، طبق قانون اهم، درست همون‌طوری که یک سیم
  واقعی رفتار می‌کند).

### فاز ۱۷ — شبیه‌سازی ظاهری نرم‌افزار واقعی پروتئوس (آیکون‌ها، نوارابزار کناری، کادر آبی بوم)
کاربر ۴ اسکرین‌شات از نرم‌افزار واقعی Proteus فرستاد و خواست ظاهر برنامه بیشتر
شبیهش بشه: آیکون به‌جای متن روی دکمه‌ها، یک نوار ابزار بالای صفحه و یک نوار
کنار صفحه، و یک کادر آبی که مرز واقعی بوم طراحی رو مشخص کنه.

- **`iconfactory.h/cpp`** (فایل جدید، کلاس `IconFactory`): چون طبق مستند پروژه
  (بخش «ابزارها و کتابخانه‌های مجاز») پیاده‌سازی گرافیکی باید فقط ++C/#C باشه و
  هیچ فایل تصویری خارجی (png/svg + .qrc) هم نمی‌خواستیم اضافه کنیم، هر آیکون
  (New/Open/Save/SaveAs/Export، Undo/Redo، Select/Wire/PlaceComponent،
  RotateCW/RotateCCW/Mirror، CheckDesign، Run/Pause/Stop/Step، ZoomIn/Out/Reset)
  مستقیماً با `QPainter` روی یک `QPixmap` کوچک (۲۴×۲۴) رسم می‌شه - خطوط ساده،
  تک‌رنگ، شبیه سبک آیکون‌های واقعی پروتئوس. هیچ dependency جدیدی اضافه نشد.
- **`mainwindow.ui`**: نوار ابزار بالا (`mainToolBar`) بازچینی شد
  (New/Open/Save/SaveAs | Undo/Redo | Zoom In/Out/Reset | Check Design) با
  `toolButtonStyle=IconOnly` - دقیقاً مثل نوار ابزار بالای پروتئوس واقعی (عکس
  سوم کاربر). یک نوار ابزار عمودی جدید کنار صفحه (`modeToolBar`, LeftToolBarArea)
  اضافه شد: Select / Wire / Place Component / Rotate CW / Rotate CCW / Mirror -
  دقیقاً مثل Sidebar آیکونی پروتئوس واقعی (عکس دوم کاربر). دو اکشن جدید
  (`actionRotateCW`, `actionRotateCCW`) و یکی (`actionMirror`) برای این نوار
  اضافه شدن - چرخش ساعتگرد/پادساعتگرد قبلاً هم توی منطق `RotateCommand` بود
  (پارامتر زاویه از قبل پشتیبانی می‌شد، فقط دکمه‌اش نبود).
- **`mainwindow.cpp`**: Run/Pause/Stop/Step به‌جای نوار بالا، حالا `QToolButton`
  با `setDefaultAction` روی نوار وضعیت پایین صفحه (سمت چپ) هستن - دقیقاً همون‌جایی
  که پروتئوس واقعی این دکمه‌ها رو نشون میده (عکس چهارم کاربر)؛ مختصات ماوس هم
  که قبلاً با `addPermanentWidget` سمت راست بود، دست‌نخورده موند. Undo/Redo چون
  متن پویا (شرح آخرین عمل) از خودِ `QUndoStack` می‌گیرن، نمی‌تونستن از قبل توی
  Designer تعریف بشن - اینجا با `insertAction` بین SaveAs و Zoom جا گرفتن.
  دو تابع جدید `on_actionRotateCW/CCW_triggered()` و `on_actionMirror_triggered()`
  دقیقاً همون منطق `keyPressEvent` (کلیدهای R/M) رو تکرار می‌کنن - نه منطق جدید،
  فقط یک مسیر ورودی دیگر (دکمه، نه فقط کیبورد) به همون `RotateCommand`/`MirrorCommand`.
- **`circuitscene.cpp`**: توی `drawBackground()`، بعد از خطوط Grid، یک کادر آبی
  (`QPen` آبی، ضخامت ۲ پیکسل با `setCosmetic(true)` تا با زوم عوض نشه) دقیقاً
  دور `sceneRect()` کشیده می‌شه - `sceneRect()` همون ابعادی‌ه که کاربر موقع
  «ایجاد پروژه جدید» (بخش ۱ مستند) انتخاب کرده، پس این کادر خودکار با هر
  اندازه بومی که کاربر بخواد درست کار می‌کنه، نه یه عدد ثابت.
- **محدودیت صادقانه:** پیش‌نمایش شماتیک کوچک بالای پنل قطعات (بخش ۳.۳ مستند،
  آیتم ۱۱ عکس دوم کاربر) و پنل ویژگی‌های زنده (بخش ۲.۱ «پیشنهادهای تکمیلی»)
  هنوز اضافه نشدن - این فاز فقط دقیقاً همون چیزی بود که کاربر این‌بار خواسته
  بود (آیکون‌ها + دو نوار ابزار + کادر آبی)، نه بازطراحی کامل لایوت.

## چیزهایی که هنوز باقی مونده (اولویت پیشنهادی)

1. **ترانزیستور NPN غیرخطی (Ebers-Moll)** — دیود (فاز ۱۴) حل شد، ولی ترانزیستور
   به‌مراتب پیچیده‌تره: دو پیوند PN جفت‌شده (BE و BC) + بهره جریان beta، یعنی
   به‌جای یک ولتاژ مجهول (مثل دیود)، دو ولتاژ مجهول جفت‌شده داره که هرکدوم روی
   هردو معادله اثر می‌ذاره. زیرساخت نیوتن-رافسون (فاز ۱۴) براش آماده‌ست ولی
   خودِ معادلات باید جدا نوشته و تک‌تک تأیید بشه.
2. **گام زمانی ثابت (Fixed Time Step) آنالوگ** — `kAnalogTimeStepSeconds=۰٫۰۰۱`
   (۱ میلی‌ثانیه) توی `sim/analogsolver.cpp` هارد‌کد شده، نه تطبیقی و نه از UI
   قابل تنظیم. برای مقادیر پیش‌فرض قطعات (مثلاً R=1kΩ، C=10µF → τ=10ms) خوب
   جواب میده، ولی مدارهایی با ثابت زمانی خیلی کوچیک‌تر یا خیلی بزرگ‌تر از ۱ms
   می‌تونن نتیجه‌ی نادرست‌تر (خطای گسسته‌سازی بیشتر) یا کند (تیک‌های خیلی زیاد
   لازم برای رسیدن به حالت ماندگار) بدن.
3. **مقاومت داخلی Battery در AnalogSolver** — الان Battery مثل یک منبع ولتاژ
   ایده‌آل حل میشه؛ ویژگی "internalResistance" ذخیره میشه ولی در معادلات MNA
   اثر نداره. برای اضافه کردنش باید یک گره داخلی مجازی هم به ازای هر Battery
   به دستگاه معادلات اضافه بشه (Battery = منبع ایده‌آل + یک Resistor سری،
   نه یک عنصر تنها) - عمداً کم‌اولویت‌تر شد چون خودِ مستند این ویژگی رو با
   «می‌تواند داشته باشد» (اختیاری) توصیف کرده، نه الزامی.
4. **Potentiometer** — هنوز اصلاً به‌عنوان کلاس مجزا وجود نداره (فقط Resistor
   ساده‌ی مقدار-ثابت داریم). با الگوی فاز ۱۶ (Switch/PushButton) قابل ساخته:
   ۳ پایه (دو سر + Wiper)، دو Resistor داخلی (Wiper-تا-سر۱ و Wiper-تا-سر۲) که
   مجموعشون ثابت می‌مونه و با ویژگی "position" (۰ تا ۱) نسبتشون عوض میشه.
5. **آدرس‌دهی بیت متغیر ADC/DAC** — تعداد بیت هردو الان ثابت (۸) و فقط زمان
   کامپایل قابل تغییره، نه از UI - مثل عرض پورت‌های MCU.
6. **کشیدن مجدد/مسیریابی چندقسمتی سیم** (drag کردن سیم موجود) — فقط
   حذف‌و‌دوباره‌کشیدن داریم.
7. **تعامل مستقیم کلیک روی Switch/PushButton/Keypad حین اجرا** (الان همه‌شون فقط
   از دیالوگ ویژگی‌ها کنترل میشن).
8. **پیش‌نمایش شماتیک قطعه بالای پنل قطعات** (بخش ۳.۳ مستند) — نرم‌افزار واقعی
   یک کادر کوچک داره که قبل از قرار دادن قطعه روی بوم، شکلش رو نشون میده (آیتم
   ۱۱ عکس Sidebar که کاربر فرستاد). الان پنل قطعات فقط لیست متنی‌ه، بدون پیش‌نمایش.

## کد مرده که هنوز توی پروژه‌ست (خودت حذف نکن مگر کاربر بخواد، ولی بدون که بی‌ربطه)

- `circuit.h/cpp` — یه کلاس `Circuit` کاملاً بلااستفاده، توی CMakeLists هست ولی
  هیچ‌جا صدا زده نمیشه.
- `wirecontroller.h/cpp`, `wireitem.h/cpp`, `wiresegment.h` — یه معماری موازی
  و کامل‌تر برای سیم‌کشی چندقسمتی که ساخته شده ولی هیچ‌وقت به `CircuitScene`
  وصل نشده. اگه قراره فاز «مسیریابی چندقسمتی سیم» رو انجام بدی، این فایل‌ها رو
  اول بخون — شاید بخشی از کار از قبل انجام‌شده باشه (تایید نشده، فقط موجوده).
## نکات پایانی برای جلسه بعدی

- کاربر همیشه zip کامل پروژه (نه فقط diff) می‌گیره؛ هر بار که فایل جدید میدی،
  از `/home/claude/proteus` (یا هرجا که پروژه رو extract کردی) کل درخت رو zip کن.
- تا وقتی کاربر تایید نکرده که Build موفق بوده، فرض کن ممکنه یه جای کوچیک خراب
  باشه — قبل از هر فاز جدید، اگه شک داری، یه بار کل فایل‌های مرتبط رو با `view`
  بخون به‌جای اینکه به حافظه‌ی این سند تکیه کنی.
- سبک پاسخ‌دهی که تا الان جواب داده: تغییرات رو مستقیم اعمال کن (نه فقط پیشنهاد
  بده)، توضیح فارسیِ خلاصه از چی‌کار شد + جدول وضعیت به‌روزشده + محدودیت‌های
  صادقانه بده، zip کامل رو با `present_files` بده.
